
import generateName from "sillyname";
import {randomSuperhero} from "superheroes"
var sillyName = generateName();

console.log(`i go by the names ${sillyName}.`);
console.log(`I am ${randomSuperhero()}`);